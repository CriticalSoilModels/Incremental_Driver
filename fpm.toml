name = "Incremental_Driver"
version = "0.1.0"
license = "BSD"
author = "Jonathan Moore"
maintainer = "jonathanm@vt.edu"
copyright = "Copyright 2024, Jonathan Moore"
[build]
auto-executables = true
auto-tests = true
auto-examples = true
module-naming = false
# external-modules = ["stdlib"]

[dependencies]

# stdlib = { git = "https://github.com/fortran-lang/stdlib", branch = "stdlib-fpm" }
stdlib = { git = "https://github.com/fortran-lang/stdlib", branch = "stdlib-fpm" }
# critical-soil-models blocked: NorSand requires aba_param.inc (Abaqus) — fix upstream first
# critical-soil-models = {git = "https://github.com/CriticalSoilModels/critical-soil-models"}
# fortplot = { git = "https://github.com/krystophny/fortplot" }
# DataFort = {git = "https://github.com/rngil/DataFort.git"}
# csv-fortran = {git = "https://github.com/jacobwilliams/csv-fortran.git"}

[dev-dependencies]
# fortplot = { git = "https://github.com/krystophny/fortplot/" }
test-drive = { git = "https://github.com/fortran-lang/test-drive" }


[install]
library = false
test = false
[fortran]
implicit-typing = false
implicit-external = false
source-form = "free"

